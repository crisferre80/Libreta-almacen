import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.libretadigital.app',
  appName: 'Libreta de almacen digital',
  webDir: 'dist'
};

export default config;
